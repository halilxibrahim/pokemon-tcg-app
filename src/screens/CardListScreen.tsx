import { View, Text,FlatList, SafeAreaView } from "react-native";

export default function CardListScreen() {
    return (
        <SafeAreaView>
            <View>
                <Text>Card List</Text>
                <FlatList
            />
            </View>
        </SafeAreaView>
    );
}       